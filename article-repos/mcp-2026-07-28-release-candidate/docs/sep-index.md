# SEP Index Extracted from the Article

This index lists Specification Enhancement Proposals mentioned in the parsed article and the role each plays in the release candidate.

| SEP | Topic |
| --- | --- |
| SEP-2575 | Removes the `initialize` / `initialized` handshake |
| SEP-2567 | Removes `Mcp-Session-Id` and protocol-level sessions |
| SEP-2260 | Restricts server-initiated requests to active client-request processing |
| SEP-2322 | Introduces Multi Round-Trip Requests and `InputRequiredResult` flow |
| SEP-2243 | Requires `Mcp-Method` and `Mcp-Name` headers for Streamable HTTP |
| SEP-2549 | Adds `ttlMs` and `cacheScope` for list and resource-read results |
| SEP-414 | Documents W3C Trace Context propagation in `_meta` |
| SEP-2133 | Formalizes the Extensions framework |
| SEP-1865 | Defines MCP Apps |
| SEP-2468 | Requires issuer validation on authorization responses per RFC 9207 |
| SEP-837 | Declares OpenID Connect `application_type` during Dynamic Client Registration |
| SEP-2352 | Binds registered credentials to authorization server issuer |
| SEP-2207 | Documents refresh-token requests for OIDC-style authorization servers |
| SEP-2350 | Clarifies scope accumulation during step-up authorization |
| SEP-2351 | Clarifies `.well-known` discovery suffix behavior |
| SEP-2577 | Defines feature lifecycle policy |
| SEP-2106 | Moves tool schemas to full JSON Schema 2020-12 |
| SEP-2164 | Changes missing-resource error to JSON-RPC `-32602 Invalid Params` |
| SEP-2484 | Requires conformance-suite scenario before Standards Track SEP Final status |

## Note

This index is derived only from the parsed article. It is not a substitute for the canonical SEP documents.
